# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in rrerrtriserv.gemspec
gemspec

group :development do
  platform :jruby do
    gem "warbler", "~> 2.0"
  end
end
